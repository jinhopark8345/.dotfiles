# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
# # Please see https://i3wm.org/docs/userguide.html for a complete reference!
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# dropbox daemon / maybe it's redundant because I have systemctl unit for dropbox daemon but I still can't see dropbox icon on system-tray
exec --no-startup-id dropbox start

# set wallpaper / using feh
exec_always feh --bg-scale ~/Dropbox/Images/wallpapers/wallpaper5-3840x2160.jpg

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet


# compton is for fading / dim / transparent effect
# but I don't use compton anymore because i3-gap and border is enough to locate my focused window
# exec --no-startup-id "compton -b"
# exec --no-startup-id /usr/bin/compton --config ~/.config/compton.conf

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# new bindings for audio control / volume control
bindsym Mod4+period exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +7% && $refresh_i3status
bindsym Mod4+comma exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -7% && $refresh_i3status
bindsym Mod4+m exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym Mod4+slash exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status

# bindsym Mod4+comma exec amixer set Master -q 3%-
# bindsym Mod4+period exec amixer set Master -q 3%+


# Use Mouse+Mod4 to drag floating windows to their wanted position
floating_modifier Mod4

# start a terminal
# bindsym Mod4+Return exec i3-sensible-terminal
bindsym Mod4+Return exec i3-sensible-terminal

# kill focused window
bindsym Mod4+Shift+q kill
bindsym Mod4+q kill

################## use rofi instead of dmenu ####################3
# start dmenu (a program launcher)
# bindsym Mod4+d exec dmenu_run
set $bg-color&amp;amp;amp;nbsp;&amp;amp;amp;nbsp; #2f343f
set $text-color #f3f4f5
bindsym Mod4+space exec rofi -show run -lines 15 -eh 1.3 -opacity "90" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$text-color" -hlfg "$bg-color" -font "System San Francisco Display 10"
################## use rofi instead of dmenu ####################3

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym Mod4+d exec --no-startup-id i3-dmenu-desktop

# change focus
# i3 uses jkl; instead of hjkl -- fix that
bindsym Mod4+h focus left
bindsym Mod4+j focus down
bindsym Mod4+k focus up
bindsym Mod4+l focus right

# alternatively, you can use the cursor keys:
bindsym Mod4+Left focus left
bindsym Mod4+Down focus down
bindsym Mod4+Up focus up
bindsym Mod4+Right focus right

# move focused window
bindsym Mod4+Shift+h move left
bindsym Mod4+Shift+j move down
bindsym Mod4+Shift+k move up
bindsym Mod4+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym Mod4+Shift+Left move left
bindsym Mod4+Shift+Down move down
bindsym Mod4+Shift+Up move up
bindsym Mod4+Shift+Right move right

# split in horizontal orientation
bindsym Mod4+slash split h
bindsym Mod4+bar split h

# split in vertical orientation
bindsym Mod4+v split v

# enter fullscreen mode for the focused container
bindsym Mod4+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# commented them out because it's hard to remember unused key bindings.
# bindsym Mod4+Shift+s layout stacking
# bindsym Mod4+Shift+t layout tabbed
# bindsym Mod4+Shift+e layout toggle split

# toggle tiling / floating
bindsym Mod4+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym Mod4+space focus mode_toggle

# focus/unfocus
bindsym Mod4+a focus parent
bindsym Mod4+c focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
set $ws14 "14"
set $ws15 "16"
set $ws16 "17"
set $ws17 "17"
set $ws18 "18"
set $ws19 "19"
set $ws20 "20"

# assign workspaces to screens
workspace 1 output DVI-D-1
workspace 2 output DVI-D-1
workspace 3 output DVI-D-1
workspace 4 output DVI-D-1
workspace 5 output DVI-D-1
# workspace 6 output DVI-D-1
# workspace 7 output DVI-D-1
# workspace 8 output DVI-D-1
# workspace 9 output DVI-D-1
# workspace 10 output DVI-D-1

workspace 6 output  HDMI-1
workspace 7 output  HDMI-1
workspace 8 output  HDMI-1
workspace 9 output  HDMI-1
workspace 10 output HDMI-1
# workspace 11 output HDMI-1
# workspace 12 output HDMI-1
# workspace 13 output HDMI-1
# workspace 14 output HDMI-1
# workspace 15 output HDMI-1
# workspace 16 output HDMI-1
# workspace 17 output HDMI-1
# workspace 18 output HDMI-1
# workspace 19 output HDMI-1
# workspace 20 output HDMI-1

# switch to workspace
bindsym Mod4+1 workspace number $ws1
bindsym Mod4+2 workspace number $ws2
bindsym Mod4+3 workspace number $ws3
bindsym Mod4+4 workspace number $ws4
bindsym Mod4+5 workspace number $ws5
bindsym Mod4+6 workspace number $ws6
bindsym Mod4+7 workspace number $ws7
bindsym Mod4+8 workspace number $ws8
bindsym Mod4+9 workspace number $ws9
bindsym Mod4+0 workspace number $ws10

# move focused container to workspace
bindsym Mod4+Shift+1 move container to workspace number $ws1
bindsym Mod4+Shift+2 move container to workspace number $ws2
bindsym Mod4+Shift+3 move container to workspace number $ws3
bindsym Mod4+Shift+4 move container to workspace number $ws4
bindsym Mod4+Shift+5 move container to workspace number $ws5
bindsym Mod4+Shift+6 move container to workspace number $ws6
bindsym Mod4+Shift+7 move container to workspace number $ws7
bindsym Mod4+Shift+8 move container to workspace number $ws8
bindsym Mod4+Shift+9 move container to workspace number $ws9
bindsym Mod4+Shift+0 move container to workspace number $ws10

# move focused container
bindsym Mod4+Shift+Control+l move container to output right
bindsym Mod4+Shift+Control+h move container to output left

# reload the configuration file
bindsym Mod4+Shift+Control+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym Mod4+Shift+Control+r restart
# exit i3 (logs you out of your X session)
bindsym Mod4+Shift+Control+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
bindsym Mod4+Control+h resize shrink width 10px or 10ppt
bindsym Mod4+Control+j resize grow height 10px or 10ppt
bindsym Mod4+Control+k resize shrink height 10px or 10ppt
bindsym Mod4+Control+l resize grow width 10px or 10ppt


# mode "resize" {
#         # These bindings trigger as soon as you enter the resize mode

#         # Pressing left will shrink the window’s width.
#         # Pressing right will grow the window’s width.
#         # Pressing up will shrink the window’s height.
#         # Pressing down will grow the window’s height.
#         bindsym h resize shrink width 10 px or 10 ppt
#         bindsym j resize grow height 10 px or 10 ppt
#         bindsym k resize shrink height 10 px or 10 ppt
#         bindsym l resize grow width 10 px or 10 ppt

#         # same bindings, but for the arrow keys
#         bindsym Left resize shrink width 10 px or 10 ppt
#         bindsym Down resize grow height 10 px or 10 ppt
#         bindsym Up resize shrink height 10 px or 10 ppt
#         bindsym Right resize grow width 10 px or 10 ppt

#         # back to normal: Enter or Escape or Mod4+r
#         bindsym Return mode "default"
#         bindsym Escape mode "default"
#         bindsym Mod4+r mode "default"
# }

# bindsym Mod4+r mode "resize"

# change lock screen image
# was using l or h to lock the screen but sometimes I lock my screen without intention so I changed it to a unpopular key
bindsym Mod4+Shift+Control+n exec i3lock -i ~/Dropbox/Images/wallpapers/wallpaper7-3840x2160.jpg

# balance i3 windows
bindsym $mod+e exec "i3_balance_workspace"

# emacsclient
# bindsym Mod4+i exec emacsclient --create-frame
bindsym Mod4+semicolon exec emacsclient --create-frame

# emacs daemon restart
# bindsym Mod4+Shift+semicolon exec systemctl --user stop emacs.service
bindsym Mod4+Shift+semicolon exec emacsclient -e '(jp/server-shutdown)'
bindsym Mod4+Shift+Control+semicolon exec systemctl --user restart emacs.service
bindsym Mod4+Shift+Control+F11 exec zenity --question --text="Do you want to power off the computer?" && shutdown now
bindsym Mod4+Shift+Control+F12 exec zenity --question --text="Do you want to restart the computer?" && shutdown -r now

exec --no-startup-id /usr/libexec/geoclue-2.0/demos/agent
exec --no-startup-id redshift-gtk

# firefox
bindsym Mod4+o exec google-chrome

# nautilus
bindsym Mod4+n exec nautilus

# Chrome
bindsym Mod4+g exec firefox

# gnome-system-monitor / win10 task-manager like
# terminal version $ top or $ lsof
# https://www.tecmint.com/command-line-tools-to-monitor-linux-performance/
bindsym Mod4+s exec gnome-system-monitor




############### Screen Shots #####################
# install scrot # $ sudo apt-get install scrot
# https://erikdubois.be/two-different-ways-make-screenshot-i3/
bindsym Print exec scrot '%Y-%m-%d-%s_screenshot_$wx$h.jpg' -e 'mv $f ~/Pictures/'
bindsym Control+Print exec gnome-screenshot -i
############### Screen Shots #####################


########### when I want to use apps in floating mode ########################
# for_window [window_role="pop-up"]                       floating enable
# for_window [window_role="bubble"]                       floating enable
# for_window [window_role="task_dialog"]                  floating enable
# for_window [window_role="Preferences"]                  floating enable
# for_window [window_type="dialog"]                       floating enable
# for_window [window_type="menu"]                         floating enable

# somehow this will cause emacsclient to pop up was floating mode
# for_window [title="jinho"]                              floating enable
for_window [class="foo"]                                floating enable
for_window [title="foo"]                                floating enable
########### when I want to use apps in floating mode ########################

# i3-gap config
# when there is only one window, there will be no border
# smart_gaps on
# smart_gaps inverse_outer

# smart_borders on
gaps inner 13
gaps outer 2

# border / border pixel=0 : no title bar
for_window [class="^.*"] border pixel 4
# new_window 1pixel

############# original i3bar config <START> ########################
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command i3status
# }
############# original i3bar config <END> ########################

# need to install bumblebee using pip
# need to install certain python packages to use bumblebee-status, instead of "git clone"
        # -m time date nic disk:root cpu memory battery pasink bluetooth brightness pasource dnf \


# indicator : show capslock or numslock
# weather : show weather, temperature
# datetime : show date and time
# cpu : show cpu usage in percentage
# sensors : cpu temperature and frequency, sensors2 and sensors need lm-sensors library
# memory : memory usage
# disk : disk usage
# battery : show remain battery percentage or AC
# nic : displays the name, IP address(es) and status of each available network interface
# pasink : Displays volume and mute status and controls for PulseAudio devices. Use wheel up and down to change volume, left click mutes, right click opens pavucontrol.
# bluetooth : Displays bluetooth status (Bluez). Left mouse click launches manager app, right click toggles bluetooth. Needs dbus-send to toggle bluetooth state.
# pasource :
# redshift
bar {
        font pango:DejaVu Sans Mono 9
        font pango:FontAwesome 9
        font pango:DejaVu Sans Mono for Powerline 9

        # get list of display from $ xrandr --query
        tray_output DVI-D-1
        # tray_output HDMI-1

        position top
        status_command ~/.config/bumblebee-status/bumblebee-status \
        -m \
           datetime weather \
           cpu sensors memory \
           disk \
           nic \
           pasink pasource \
           battery bluetooth \
           redshift \
           brightness \
           emacs dropbox indicator \
        -p root.path=/ \
           time.format="%H:%M CW %V" \
           date.format="%a, %b %d %Y"  \
           datetime.format="%H:%M %a %b %d %Y CW:%V" \
           indicator.include='capslock' \
           memory.format='{used}/{total}' \
           disk.format='{used}/{size}' \
           nic.format='{intf}/{state}' \
           emacs.interval=3 \
           dropbox.interval=3 \
        -t iceberg-contrast

}

